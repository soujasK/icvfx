import React from 'react';
import ReactDOM from 'react-dom/client';
import StageMissionControl from '../StageMissionControl.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StageMissionControl />
  </React.StrictMode>
);
